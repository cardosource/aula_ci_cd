name: Github Action - Variaveis de ambiente
run-name: Executando - Variavel de Ambiente

on: [push,workflow_dispatch]
env:
  variavel: "conteudo aqui"
jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: iniciado
        run: |
         echo "$variavel "
