name: Github Action - ignore  (basic)
run-name: Executando - ${{github.event_name}}

on:
  push:
    paths:
     - '**.py'
jobs:
  on-push:
   runs-on: ubuntu-latest
   steps:
     -  name: Comentario
        run: echo "arquivos python"
